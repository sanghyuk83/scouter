credential:
  - name: HOST
    value: {{ include "scouterapm.primary.fullname" . }}.{{ .Release.Namespace }}.svc.{{ .Values.clusterDomain }}
  - name: PORT
    valueFrom:
      serviceRef:
        name: {{ include "scouterapm.primary.fullname" . }}
        jsonpath: '{ .spec.ports[?(@.name=="scouterapm")].port }'
{{- if (eq .Values.primary.service.type "NodePort") }}
  - name: NODE_PORT
    valueFrom:
      serviceRef:
        name: {{ include "scouterapm.primary.fullname" . }}
        jsonpath: '{ .spec.ports[?(@.name=="scouterapm")].nodePort }'
{{- end }}
  - name: CLUSTER_IP
    valueFrom:
      serviceRef:
        name: {{ include "scouterapm.primary.fullname" . }}
        jsonpath: '{ .spec.clusterIP }'
